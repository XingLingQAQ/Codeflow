{
  "name": "gui-legacy",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "packages/gui/src",
  "tags": ["scope:libs", "lang:ts", "status:legacy"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --filter gui build",
        "cwd": "."
      },
      "inputs": ["production"],
      "outputs": ["{projectRoot}/dist"]
    }
  }
}
