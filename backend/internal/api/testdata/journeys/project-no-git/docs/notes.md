# Notes

This is a plain text document inside the project-no-git fixture.

- It has no version control history.
- A copy-based shadow must track it through the file manifest and its hash.
- Editing this file inside a shadow copy must never touch the fixture source.
