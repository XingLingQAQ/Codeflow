# dependencies (never commit)
node_modules/

# local tooling / caches
.ace-tool/
.nx/
.tmp-playwright/
tmp-playwright/
artifacts/
.archive/
*.log
*.err

# local worktrees and tool state
.claude/worktrees/
.cunzhi-memory/
.pnpm-home/
plan/

# backend local outputs
backend/.claude/
backend/coverage
backend/nul

# app / embed build outputs (must not be committed)
apps/**/dist/
backend/internal/web/dist/

# package local build outputs
# NOTE: packages/core|gui|shared|ui-components dist may remain tracked intentionally
# until CI always builds them before consumers. packages/cli dist is always local.
packages/cli/dist/
packages/cli/tsconfig.tsbuildinfo

# coverage reports
coverage/
